import { SelectControlDirective } from './select-control.directive';

describe('SelectControlDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectControlDirective();
    expect(directive).toBeTruthy();
  });
});
